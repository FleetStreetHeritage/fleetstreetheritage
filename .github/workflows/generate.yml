name: Generate site

on:
  push:
    branches: [main]

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - uses: actions/checkout@v4.2.2

      - uses: actions/setup-python@v5.4.0
        with:
          python-version: '3.12'

      - name: Generate staging output (always)
        working-directory: fsh_reboot/scripts
        run: python generate.py

      - name: Publish draft → content (if PUBLISH flag present)
        id: publish
        run: |
          if [ -f fsh_reboot/editor/content_draft/PUBLISH ]; then
            echo "PUBLISH flag found — promoting content_draft to content"
            cp fsh_reboot/editor/content_draft/*.md fsh_reboot/content/
            rm fsh_reboot/editor/content_draft/PUBLISH
            cd fsh_reboot/scripts && python generate.py
            echo "published=true" >> "$GITHUB_OUTPUT"
          else
            echo "No PUBLISH flag — draft commit only"
            echo "published=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Commit generated output
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/dev/ docs/qr/ fsh_reboot/content/ fsh_reboot/editor/content_draft/
          if git diff --cached --quiet; then
            echo "Nothing to commit"
          else
            if [ "${{ steps.publish.outputs.published }}" = "true" ]; then
              # Deliberately NOT [skip ci]: this is what lets Deploy to
              # Production's own push-trigger (on fsh_reboot/content/**)
              # fire and put the newly-published homepage copy live.
              git commit -m "chore: publish content update"
            else
              git commit -m "chore: regenerate site [skip ci]"
            fi
            git push
          fi
